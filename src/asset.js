import * as THREE from 'three';

// Object of factory and anonymous functions for creating assets
const assets = {
    'grass': (x, y) => {},
    'building-1': (x, y) => {},
    'building-2': (x, y) => {},
    'building-3': (x, y) => {}
}